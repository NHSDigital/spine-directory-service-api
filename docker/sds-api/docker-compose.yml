version: '3'

services:
  route:
    image: local/spine-directory-service:${BUILD_TAG}
    ports:
        - "9000:80"
    environment:
      - SDS_LOG_LEVEL=NOTSET
      - SDS_SDS_URL=ldap://192.168.128.11
      - SDS_SDS_SEARCH_BASE=ou=services,o=nhs
      - SDS_DISABLE_SDS_TLS=True
      - SDS_SDS_REDIS_CACHE_HOST=cache-service
      - SDS_SDS_REDIS_DISABLE_TLS=True

  cache-service:
    image: local/cache-service:${BUILD_TAG}
    ports:
          - "6379:6379"
