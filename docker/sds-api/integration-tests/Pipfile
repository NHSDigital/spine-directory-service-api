[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
requests = "*"

[requires]
python_version = "3.7"

[scripts]
inttests = 'python -m xmlrunner discover -o test-reports -p "int_*" -v'
